# Bytecode Encoder UI

Flutter desktop GUI for Bytecode PHP Loader.

The UI is part of the main Bytecode PHP Loader repository and is licensed under
Apache-2.0. See the repository root `LICENSE` and `NOTICE` files.
